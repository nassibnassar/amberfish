#ifndef _AF_IO_H
#define _AF_IO_H

/*
#include <stdio.h>

#define AF_FILE FILE

static inline AF_FILE *af_fopen(const char *path, const char *mode)
{
	return fopen(path, mode);
}

static inline int af_fclose(AF_FILE *stream)
{
	return fclose(stream);
}

static inline size_t af_fread(void *ptr, size_t size, size_t nmemb, AF_FILE *stream)
{
	return fread(ptr, size, nmemb, stream);
}

static inline size_t af_fwrite(const void *ptr, size_t size, size_t nmemb, AF_FILE *stream)
{
	return fwrite(ptr, size, nmemb, stream);
}

static inline int af_fseek(AF_FILE *stream, long offset, int whence)
{
	return fseek(stream, offset, whence);
}
*/

#endif
